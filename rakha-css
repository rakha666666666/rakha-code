        :root {
            --bg-pink-light: #ffe6f0;
            --bg-pink-gradient: linear-gradient(135deg, #ff9a9e 0%, #fecfef 99%, #fecfef 100%);
            --primary-pink: #ff477e;
            --dark-pink: #d90429;
            --white: #ffffff;
            --text-color: #590d22;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: var(--bg-pink-gradient);
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            overflow: hidden;
            color: var(--text-color);
        }

        /* Efek Latar Belakang Hati Jatuh */
        .heart-bg {
            position: absolute;
            top: -10vh;
            color: rgba(255, 71, 126, 0.4);
            font-size: 1.5rem;
            animation: fall linear infinite;
            z-index: 1;
        }

        @keyframes fall {
            0% {
                transform: translateY(-10vh) rotate(0deg);
                opacity: 1;
            }
            100% {
                transform: translateY(110vh) rotate(360deg);
                opacity: 0;
            }
        }

        /* Container Utama */
        .card-container {
            position: relative;
            z-index: 10;
            background: rgba(255, 255, 255, 0.85);
            backdrop-filter: blur(10px);
            padding: 40px;
            border-radius: 30px;
            box-shadow: 0 15px 35px rgba(255, 71, 126, 0.2);
            text-align: center;
            max-width: 450px;
            width: 90%;
            border: 2px solid rgba(255, 255, 255, 0.5);
            transition: transform 0.3s ease;
        }

        .card-container:hover {
            transform: translateY(-5px);
        }

        /* Halaman / Slide */
        .page {
            display: none;
            animation: fadeIn 0.8s ease-in-out forwards;
        }

        .page.active {
            display: block;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }

        /* Tipografi */
        h1 {
            font-family: 'Sacramento', cursive;
            font-size: 3.5rem;
            color: var(--primary-pink);
            margin-bottom: 10px;
            line-height: 1.2;
        }

        h2 {
            font-size: 1.3rem;
            margin-bottom: 15px;
            color: var(--text-color);
        }

        p {
            font-size: 0.95rem;
            line-height: 1.6;
            margin-bottom: 25px;
            color: #72093b;
        }

        /* Tombol Navigasi */
        .btn-container {
            display: flex;
            justify-content: center;
            gap: 10px;
        }

        .btn {
            background: var(--primary-pink);
            color: var(--white);
            border: none;
            padding: 12px 24px;
            font-size: 0.95rem;
            font-weight: 600;
            border-radius: 50px;
            cursor: pointer;
            box-shadow: 0 5px 15px rgba(255, 71, 126, 0.4);
            transition: all 0.3s ease;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn:hover {
            background: var(--dark-pink);
            transform: scale(1.05);
            box-shadow: 0 8px 20px rgba(217, 4, 41, 0.4);
        }

        .btn-secondary {
            background: #ffa0b4;
        }

        /* Foto Profil / Ilustrasi untuk Setiap Halaman */
        .image-box {
            width: 130px;
            height: 130px;
            margin: 0 auto 20px auto;
            border-radius: 50%;
            overflow: hidden;
            border: 4px solid var(--primary-pink);
            box-shadow: 0 5px 15px rgba(0,0,0,0.1);
            animation: fadeIn 0.8s ease-in-out;
        }

        .image-box img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        /* Lilin & Kue Icon */
        .cake-icon {
            font-size: 3.5rem;
            color: var(--primary-pink);
            margin-bottom: 15px;
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }
    
